{
  "name": "node-docker-app",
  "description": "first node app for docker",
  "version": "0.0.1",
  "license": "use as you like",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
